import styles from './Timer.module.scss';

const Timer = () => {
     return (
          <div className={styles.timer}>
               <h1>00:00:00.000</h1>
          </div>
     );
}

export default Timer;